name: proveo
version: 1.0.0+1
description: Proveo app para conectar proveedores y compradores PYMES.
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.10
  supabase_flutter: ^2.16.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
